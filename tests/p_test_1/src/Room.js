import { Shader, VAO, VBO } from 'Akila/webgl';

export class RoomRenderer {
    constructor(vs, fs) {
        this.shader = new Shader(vs, fs, 'Room');

         
    }
}

export class Room {
    constructor() {
        this.shader
    }
}
